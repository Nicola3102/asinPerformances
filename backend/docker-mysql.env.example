# 仅用于 docker-compose 中的 mysql 服务（勿与 backend/.env 混用，避免线上库口令进入 DB 容器）。
# 使用：cp docker-mysql.env.example docker-mysql.env 并填写；docker-mysql.env 已纳入 .gitignore。
#
# 须与 backend/.env 中的 MYSQL_DATABASE / MYSQL_USER / MYSQL_PASSWORD 保持一致（应用连库与镜像初始化用户一致）。
MYSQL_ROOT_PASSWORD=
MYSQL_DATABASE=
MYSQL_USER=
MYSQL_PASSWORD=

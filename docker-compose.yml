services:
  mysql:
    image: mysql:8.0
    environment:
      MYSQL_DATABASE: rug
      MYSQL_USER: rug_user
      MYSQL_PASSWORD: rug_password
      MYSQL_ROOT_PASSWORD: root_password
    ports:
      - "3307:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-proot_password"]
      interval: 5s
      timeout: 5s
      retries: 10

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    volumes:
      - ./backend:/app
    ports:
      - "9090:9090"
    env_file:
      - ./backend/.env
    environment:
      MYSQL_HOST: mysql
      MYSQL_PORT: 3306
      MYSQL_USER: rug_user
      MYSQL_PASSWORD: rug_password
      MYSQL_DATABASE: rug
    command: uvicorn app.main:app --host 0.0.0.0 --port 9090 --reload
    depends_on:
      mysql:
        condition: service_healthy

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    volumes:
      - ./frontend:/app
      - frontend_node_modules:/app/node_modules
    ports:
      - "5173:5173"
    environment:
      - VITE_API_HOST=backend
      - VITE_API_PROXY_TARGET=http://backend:9090
    depends_on:
      - backend

volumes:
  mysql_data:
  frontend_node_modules:
